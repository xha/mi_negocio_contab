@model Datos.PlanillaModels.PMaestraModel

<input id="parametro" name="parametro" type="hidden" value="@ViewBag.accion" />
<div class="container-fluid">
    <div class="row">
        <div class="col-sm-12">
            <a href="#" onclick="AjaxModalGet('Configuración de Archivo de Sincronización', '/PAsistencia/DetalleArchivoSincronizacion', '', 'm_middle');" class="btn btn-success">
                <i class='fa fa-plus'></i>
                Agregar
            </a>
        </div>
    </div>
    <br />
    <div class="card shadow">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-bordered table-striped pre-scrollable datatables" id="ArchivoSincronizacionGrid" cellspacing="0" width="100%">
                    <thead>
                        <tr>
                            <th>Nro. Archivo</th>
                            <th>Ruta Archivo</th>
                            <th>Nombre</th>
                            <th>Tabulador</th>
                            <th style="width: 5px"></th>
                        </tr>
                    </thead>
                    <tbody></tbody>
                </table>
            </div>
        </div>
    </div>
    <hr />
    <div class="form-group row">
        <div class="col-sm-12">
            <a href="#" onclick="$('#m_central').modal('hide')" class="btn btn-info">
                <i class='fa fa-arrow-left'></i>
                Regresar
            </a>
        </div>
    </div>
</div>

<script>
    $(function () {
        /*$("#ArchivoSincronizacionGrid").DataTable({
            processing: true, // for show progress bar
            serverSide: true, // for process server side
            filter: true, // this is for disable filter (search box)
            orderMulti: false, // for disable multiple column at once


            ajax: {
                url: ruta_actual + "/PMaestra/GridConceptos",
                type: "POST",
                datatype: "json",
            },
            columnDefs:
            [{
                targets: [2,3,4],
                orderable: false,
                searchable: false
            }],
            columns: [
                { data: "CODIGO_CONCEPTO" },
                { data: "DESCRIPCION_CONCEPTO" },
                { data: "COLUMNA_PLANILLA" },
                { data: "FORMULA" },
                {
                    render: function (data, type, row)
                    { return '<a class="btn btn-info grid_opt" title="Editar" href="#" onclick="AjaxModalGet(\'Registro de Incidencia\', \'/PAsistencia/DetalleIncidencia\', \'' + row.CODIGO_CONCEPTO + '\', \'m_middle\');"><i class="fa fa-edit"></i></a>'; }
                },
                {
                    data: null, render: function (data, type, row) {
                        return '<a class="btn btn-danger grid_opt" title="Anular" href="#" onclick="anular_nota(\'' + row.CODIGO_CONCEPTO + '\')"><i class="fa fa-trash"></i></a>';
                    }
                },
            ]
        });*/
    });
</script>
